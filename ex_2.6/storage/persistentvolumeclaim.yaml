apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-app-pvc
  namespace: project
spec:
  storageClassName: todo-app-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
